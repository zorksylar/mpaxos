{
    "_comments": "nodename is optional, it can be set via API. 
                    flush option could be: ABANDON, MEM, ASYNC, SYNC",
    "nodename": "node1",
    "flush": "ABANDON",
    "nodes":[
    {
        "name": "node1",
        "addr": "localhost",
        "port": 8881
    }
    ]
}
